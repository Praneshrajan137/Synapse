{
  "$description": "SYNAPSE Chromatic — semantic tokens. Meaning-bound, theme-aware aliases. These are the tokens emitted as CSS custom properties and Tailwind utilities. Components reference ONLY these, never primitives.",
  "color": {
    "$type": "color",
    "surface": {
      "$description": "Elevation ramp — canvas (page) to overlay (modals/popovers). v1.2.0 Obsidian: deeper canvas, LARGER lightness steps so elevation reads through surface contrast alone (borderless depth, ADR-045).",
      "canvas": { "$value": { "light": "{ref.neutral.965}", "dark": "{ref.neutral.135}" } },
      "panel": { "$value": { "light": "{ref.neutral.990}", "dark": "{ref.neutral.180}" } },
      "raised": { "$value": { "light": "{ref.neutral.990}", "dark": "{ref.neutral.225}" } },
      "overlay": { "$value": { "light": "{ref.neutral.990}", "dark": "{ref.neutral.285}" } }
    },
    "border": {
      "$description": "Borders are EARNED in the Obsidian system (ADR-045): interactive affordances, table rules, and alert states — never default panel chrome (dark theme; light keeps hairlines; hc keeps strong borders).",
      "subtle": { "$value": { "light": "{ref.neutral.925}", "dark": "{ref.neutral.340}" } },
      "strong": { "$value": { "light": "{ref.neutral.880}", "dark": "{ref.neutral.420}" } }
    },
    "text": {
      "$description": "Type hierarchy — primary (body), secondary (labels), tertiary (muted), inverse (on coloured fills). v1.2.0 widens the secondary/tertiary spread so the hierarchy is visible, not just measurable.",
      "primary": { "$value": { "light": "{ref.neutral.240}", "dark": "{ref.neutral.972}" } },
      "secondary": { "$value": { "light": "{ref.neutral.420}", "dark": "{ref.neutral.790}" } },
      "tertiary": { "$value": { "light": "{ref.neutral.500}", "dark": "{ref.neutral.700}" } },
      "inverse": { "$value": { "light": "{ref.neutral.990}", "dark": "{ref.neutral.100}" } }
    },
    "brand": {
      "$description": "SYNAPSE system identity — the royal-cobalt cognition hue (H250, v2.0.0/ADR-048); shares the tier family. The accent is RATIONED to one primary action per surface ('quiet foundation, loud meaning'). The contrast structure is unchanged from the gold era, only the hue rotates to cobalt: dark theme uses a bright mid-cobalt base carrying a near-black label (clears APCA secondary + WCAG AA); light theme uses a deeper cobalt base carrying a white label.",
      "base": { "$value": { "light": "oklch(0.48 0.160 250)", "dark": "oklch(0.78 0.130 250)" } },
      "emphasis": { "$value": { "light": "oklch(0.42 0.165 250)", "dark": "oklch(0.85 0.100 250)" } },
      "fg": { "$value": { "light": "{ref.neutral.990}", "dark": "{ref.neutral.100}" } }
    },
    "focus": {
      "ring": { "$value": { "light": "oklch(0.52 0.150 250)", "dark": "oklch(0.72 0.150 250)" } }
    },
    "agent": {
      "$description": "8 agent identities — categorical, Hue axis. Scoped to identity affordances only.",
      "demand-prophet": { "$value": "{ref.agent.demand_prophet}" },
      "routing-navigator": { "$value": "{ref.agent.routing_navigator}" },
      "inventory-sentinel": { "$value": "{ref.agent.inventory_sentinel}" },
      "freshness-guardian": { "$value": "{ref.agent.freshness_guardian}" },
      "pricing-oracle": { "$value": "{ref.agent.pricing_oracle}" },
      "disruption-shield": { "$value": "{ref.agent.disruption_shield}" },
      "supplier-trust": { "$value": "{ref.agent.supplier_trust}" },
      "sustainability-agent": { "$value": "{ref.agent.sustainability_agent}" }
    },
    "tier": {
      "$description": "4 decision tiers — ordinal, Lightness axis (I-8).",
      "1": { "$value": "{ref.tier.1}" },
      "2": { "$value": "{ref.tier.2}" },
      "3": { "$value": "{ref.tier.3}" },
      "4": { "$value": "{ref.tier.4}" }
    },
    "confidence": {
      "$description": "Confidence zones bounded by the I-5 gates. low < 0.70 <= escalation < 0.80 <= autonomous; peak is the 1.00 anchor of the continuous scale.",
      "low": { "$value": "{ref.confidence.low}" },
      "escalation": { "$value": "{ref.confidence.caution}" },
      "autonomous": { "$value": "{ref.confidence.gain}" },
      "peak": { "$value": "{ref.confidence.high}" }
    },
    "state": {
      "$description": "State feedback — separated from agent colours by lightness/chroma (INV-CLR-014/015). degraded + synthetic are the v1.1.0 honesty states (ADR-044): degraded marks any output produced on a fallback path (chroma-drained, INV-CLR-016); synthetic marks traffic-generator decisions.",
      "success": { "$value": "{ref.state.success}" },
      "warning": { "$value": "{ref.state.warning}" },
      "danger": { "$value": "{ref.state.danger}" },
      "info": { "$value": "{ref.state.info}" },
      "neutral": { "$value": "{ref.state.neutral}" },
      "degraded": { "$value": "{ref.state.degraded}" },
      "synthetic": { "$value": "{ref.state.synthetic}" }
    },
    "hitl": {
      "$description": "Human-in-the-loop decision outcomes (I-5 escalation console).",
      "approve": { "$value": "{ref.state.success}" },
      "reject": { "$value": "{ref.state.danger}" },
      "modify": { "$value": "{ref.state.warning}" },
      "pending": { "$value": "{ref.state.info}" }
    }
  },
  "motion": {
    "$description": "Motion tokens — all consuming components must respect prefers-reduced-motion (INV-CLR-013). v1.1.0 (ADR-044): urgent is the escalation pulse — exactly half the 2400ms ambient pulse, because urgency is encoded as FREQUENCY, not a new colour; arrive is the decision-arrival entrance, between base and slow; entrance is a hard-decelerate curve for elements entering the stage.",
    "duration": {
      "$type": "duration",
      "fast": { "$value": "120ms" },
      "base": { "$value": "200ms" },
      "slow": { "$value": "360ms" },
      "arrive": { "$value": "280ms" },
      "urgent": { "$value": "1200ms" },
      "pulse": { "$value": "2400ms" }
    },
    "ease": {
      "$type": "cubicBezier",
      "standard": { "$value": "cubic-bezier(0.4, 0, 0.2, 1)" },
      "decelerate": { "$value": "cubic-bezier(0, 0, 0.2, 1)" },
      "emphasized": { "$value": "cubic-bezier(0.4, 0, 0.6, 1)" },
      "entrance": { "$value": "cubic-bezier(0.05, 0.7, 0.1, 1)" }
    }
  },
  "factor": {
    "$description": "Agent process-state grammar (ADR-044): identity hue is FROZEN (INV-CLR-012); what an agent is DOING is encoded as chroma rationing — a scalar factor applied to the identity colour's chroma (the rationedAgentColor pattern). Strictly monotone with cognitive activity (INV-CLR-017): an interrupted agent is nearly achromatic, an acting agent burns at full identity. escalated stays at full chroma and composes with the danger ring + urgent pulse (a composition rule, not a colour). The Atlas Console Elevation canonical Agent-State Model (Req 2.7, INV-CLR-017) extends this grammar to every observable lifecycle state; each factor is authored HERE (never invented in TS), and every factor is strictly > 0 so identity hue is never chroma-drained to neutral gray — including the interrupting/failing states (interrupting floor = 0.25).",
    "agentstate": {
      "$type": "number",
      "interrupted": { "$value": 0.25 },
      "interrupting": { "$value": 0.25 },
      "idle": { "$value": 0.3 },
      "waiting": { "$value": 0.35 },
      "asking": { "$value": 0.4 },
      "uncertain": { "$value": 0.45 },
      "recovering": { "$value": 0.5 },
      "thinking": { "$value": 0.55 },
      "searching": { "$value": 0.6 },
      "planning": { "$value": 0.65 },
      "delegating": { "$value": 0.7 },
      "handing-off": { "$value": 0.72 },
      "debating": { "$value": 0.8 },
      "streaming": { "$value": 0.85 },
      "confident": { "$value": 0.9 },
      "acting": { "$value": 1 },
      "escalating": { "$value": 1 },
      "escalated": { "$value": 1 },
      "failing": { "$value": 1 },
      "succeeding": { "$value": 1 },
      "completing": { "$value": 1 }
    }
  },
  "radius": {
    "$type": "dimension",
    "sm": { "$value": "0.375rem" },
    "md": { "$value": "0.5rem" },
    "lg": { "$value": "0.75rem" },
    "xl": { "$value": "1rem" },
    "full": { "$value": "9999px" }
  }
}
