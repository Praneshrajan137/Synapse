node_modules
dist
# The e2e-harness build (`pnpm build:e2e`, mode `e2e`). Kept OUT of `dist/` on
# purpose: `dist/` is the shipped bundle and AD-12 asserts the harness global is
# absent from it (scripts/audit/workflow_shape_truth.py).
dist-e2e
coverage
.vite
.cache
.storybook-static
playwright-report
test-results
*.log
.DS_Store
.env
.env.local
.env.*.local
.turbo
src/transport/openapi.gen.ts
src/domain/*.generated.ts

# Mutation testing (Stryker)
.stryker-tmp
reports
stryker.log

# Supply-chain artifacts (regenerated by CI each run)
sbom.cdx.json

# Contract Fidelity drift artifacts (regenerated by `pnpm contract:drift`)
spec/contract-fidelity/drift-report.json
spec/contract-fidelity/drift-report.md
