pytest>=8.2.0,<9.0
pytest-asyncio>=0.23.0,<1.0
pytest-cov>=5.0.0,<6.0
hypothesis>=6.100.0,<7.0
mypy>=1.10.0,<2.0
# mypy imports `pathspec.patterns.gitignore` (added in pathspec 0.10.0). A
# transitive resolve to an older pathspec makes mypy crash on import with
# `ModuleNotFoundError: No module named 'pathspec.patterns.gitignore'`. Pin a
# compatible floor so the MyPy CI step runs instead of crashing at startup.
pathspec>=0.10.0
ruff==0.14.14
deal>=4.24.0,<5.0
mutmut>=2.4.0,<3.0
whatthepatch>=1.0.7,<2.0
schemathesis>=3.30.0,<4.0
