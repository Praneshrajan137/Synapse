pydantic>=2.7.0,<3.0
confluent-kafka>=2.4.0,<3.0
httpx>=0.27.0,<1.0
prometheus-client>=0.20.0,<1.0
structlog>=24.1.0,<25.0
# RS256 JWT verifier (synapse_common.auth.jwt_manager); the [cryptography]
# extra pulls the RSA backend used for key generation / JWKS export.
python-jose[cryptography]>=3.3.0,<4.0
