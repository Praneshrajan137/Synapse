# SYNAPSE Orchestrator — Sprint 4 pinned dependencies
# I-1: Zero paid APIs. All open-source or free-tier.
fastapi==0.111.0
# fastapi 0.111.0 passes `on_startup`/`on_shutdown` to starlette's Router; that
# kwarg was removed in starlette 0.38. Unpinned, pip resolves a newer starlette
# and api/ imports crash with `Router.__init__() got an unexpected keyword
# argument 'on_startup'`. Pin the compatible range fastapi 0.111 declares.
starlette>=0.37.2,<0.38.0
uvicorn[standard]==0.30.1
httpx==0.27.0
pydantic==2.7.4
pydantic-settings==2.3.4
sqlalchemy[asyncio]==2.0.31
asyncpg==0.29.0
pymoo==0.6.1.3
deal==4.24.4
confluent-kafka>=2.5.0
redis==5.0.7
neo4j==5.22.0
pinecone==5.0.1
prometheus-client==0.20.0
websockets==12.0
structlog>=24.1.0
opentelemetry-api==1.25.0
opentelemetry-sdk==1.25.0
opentelemetry-instrumentation-fastapi==0.46b0
numpy>=1.26.0
