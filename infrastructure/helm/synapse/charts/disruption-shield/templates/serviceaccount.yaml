apiVersion: v1
kind: ServiceAccount
metadata:
  name: disruption-shield
  labels:
    app: disruption-shield
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: false
